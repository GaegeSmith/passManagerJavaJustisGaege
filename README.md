# passManagerJavaJustisGaege

MUST COMPILE WITH THIS COMMAND

javac -encoding UTF-8 *.java

There are characters that are not supported by the default text encoding (which we are pretty sure is very old)