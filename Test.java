import java.util.ArrayList;

public class Test {
    public static void main(String[] args) {
        System.out.println("What do you want your password to be, must be 8 characters with at least one of each of these:  UPPERCASE LETTER, lowercase letter, number 0-9, and special characters \"!\", \"@\", \"#\", \"$\", \"%\", \"^\", \"&\", \"(\", \")\", or type \"gen\" to generate one automagically: ");
        System.exit(0);
        System.out.println("Hello there");
    }
}
